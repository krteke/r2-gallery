# Environment variables for R2 Gallery

# Credentials for logging into the gallery
# The PASSWORD must be the hash generated by the command in the README
USERNAME="admin"
PASSWORD='your_generated_password_hash'

# A long, random, and secret string for signing authentication tokens
JWT_SECRET="a_long_random_string_for_jwt_secret"

# Log level (e.g., info, debug)
RUST_LOG="info"

# --- Cloudflare R2 Configuration ---
# Your Cloudflare Account ID
R2_ACCOUNT_ID="your_account_id"

# The region of your R2 bucket (usually "auto")
R2_REGION="auto"

# The name of your R2 bucket
R2_BUCKET_NAME="your_bucket_name"

# The public URL for your R2 bucket (can be the R2.dev URL or your custom domain)
R2_URL="https://pub-....r2.dev"


# --- Docker Compose Configuration ---
# The port the application listens on INSIDE the container
PORT=8099

# The port on your HOST machine to map to the container's port
HOST_PORT=8080

# A descriptive name for your container
CONTAINER_NAME=r2-gallery
